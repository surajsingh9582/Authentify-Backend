# Authentify – Backend

A Java Spring Boot backend application for secure authentication and authorization.

## Features
- User registration and login
- JWT-based authentication
- Password change & reset
- Email-based account verification
- Role-based access control
- Secure REST APIs

## Tech Stack
- Java
- Spring Boot
- Spring Security
- JWT
- MySQL
- Maven

## Configuration
Create an `application.properties` file in `src/main/resources`
using `application.properties.example` as reference and
update it with your local database and email credentials.

## Run Locally
```bash
mvn spring-boot:run
